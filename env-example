APP_ENV= # production | dev
PORT=
LISTEN_HOST= # empty = localhost; Docker deployment sets 0.0.0.0 (compose publishes 127.0.0.1:8080 only)
DBUSER=
DBPASS=
DBHOST=
DBPORT=
DBNAME=
DBNAME_TEST=
DOMAIN=
IMG_PATH= # Docker deployment uses /data/images (bind mount of /home/Zhou/apps/anoweb/images)
IMG_URL_PREFIX=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=
FRONTEND_URL=

# Email
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# MySQL container (read by docker-compose only, not the app;
# DBUSER/DBPASS/DBNAME above are also reused as the container's credentials)
MYSQL_ROOT_PASSWORD=